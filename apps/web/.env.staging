# When adding additional environment variables, the schema in "src/config/index.ts"
# should be updated accordingly.

NEXT_PUBLIC_API_URL=https://api.ship-test-task.wip.la
NEXT_PUBLIC_WS_URL=https://api.ship-test-task.wip.la
NEXT_PUBLIC_WEB_URL=https://web.ship-test-task.wip.la

# Mixpanel
# NEXT_PUBLIC_MIXPANEL_API_KEY=...
